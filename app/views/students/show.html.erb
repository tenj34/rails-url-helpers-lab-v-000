<h1><%= @student.to_s %></h1>
<p>
  <% if @student.active? %>
    <%= "This student is currently active" %>
  <%else%>
    <%= "This student is not currently active" %>
  <%end%>

  <%= @student.active %>
</p>